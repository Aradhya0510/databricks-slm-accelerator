# Local Development Dependencies
#
# Install the project for development:
#     pip install -e ".[dev]"
#
# Kept for compatibility with existing docs that reference it.

# Core runtime deps (also installed on the cluster by jobs/*.py entry points):
-r requirements_runtime.txt

# Evaluation
rouge-score>=0.1.2

# Dev / testing
pytest>=7.0
pytest-cov>=4.0

# Config
pyyaml>=6.0
pydantic>=2.0
